// -------------------------------------------------------------
// Encoders

// Note: `encrypted_text` variable has the latest user text input (refer: main.js).

function algorithm1(pixel3DArray) {
    const temporaryUpsideDownImage = pixel3DArray.reverse();
    return encodeBMPFrom3dData(temporaryUpsideDownImage);
}

function algorithm2(pixel3DArray) {
    const temporaryUpsideDownImage = pixel3DArray.reverse();
    return encodeBMPFrom3dData(temporaryUpsideDownImage);
}

function algorithm3(pixel3DArray) {
    const temporaryUpsideDownImage = pixel3DArray.reverse();
    return encodeBMPFrom3dData(temporaryUpsideDownImage);
}
