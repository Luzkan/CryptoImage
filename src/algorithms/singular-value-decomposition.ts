// -------------------------------------------------------------
// Singular value decomposition algorithm

let blockSize = 8;

function singularValueDecompositionEncrypt(pixel3DArray: [number, number, number, number][][]) {

    // const arrays = pixel3DArrayTo2DArrays(pixel3DArray)

    // arrays.forEach(encrypt2DArray(arrays))

    // return twoDimArraysToPixel3DArray(arrays)
}

function pixel3DArrayTo2DArrays(pixel3DArray: [number, number, number, number][][]) {

    // convert pixel 3D array to three 2D arrays

    return [[], [], []];
}

function encrypt2DArray(twoDimArray: any) {

    // blocks = twoDimArrayToBlocks(twoDimArray)
    // blocks.forEach(encryptInBlock)
    // overwriteBlocks(twoDimArray, blocks)
}

function twoDimArrayToBlocks(twoDimArray: any) {

    // convert 2D array to blocks n x n (n = blockSize)

    return [];
}

function encryptInBlock(block: any) {

    // encrypt message in block
}

function overwriteBlocks(twoDimArray: any, blocks: any) {

    // overwrite blocks of 2D array with encrypted blocks
}

function twoDimArraysToPixel3DArray(twoDimArrays: any) {

    // convert three 2D arrays to pixel 3D array

    // const temporaryUpsideDownImage = pixel3DArray.reverse();
    // return encodeBMPFrom3dData(temporaryUpsideDownImage);
}